services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_USER=postgres
      - PGUSER=postgres
      - POSTGRES_NAME=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
# shell> PGPASSWORD=postgres psql -U postgres -h localhost postgres
